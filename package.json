{
  "name": "spine-physics-viewer",
  "version": "1.0.0",
  "description": "Spine 4.1 model viewer with NIKKE physics configuration",
  "main": "./index.ts",
  "scripts": {
    "dev": "cross-env NODE_ENV=development vite",
    "start": "npm run dev",
    "build": "cross-env NODE_ENV=production vite build",
    "preview": "cross-env NODE_ENV=production vite preview",
    "deploy": "npm run build && gh-pages -d dist --add"
  },
  "dependencies": {
    "@pixi-spine/all-4.1": "^4.0.6",
    "@types/pixi.js": "^5.0.0",
    "pixi.js": "7.4.3",
    "vite": "^7.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "cross-env": "^7.0.3",
    "gh-pages": "^6.3.0",
    "typescript": "^5.1.6"
  }
}
